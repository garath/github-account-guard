{
  "name": "github-account-guard",
  "version": "1.0.0",
  "private": true,
  "description": "A Microsoft Edge extension that warns when GitHub uses an unexpected account.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/validate.js"
  }
}
